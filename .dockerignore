# =============================================================================
# .dockerignore - docker build 时忽略的文件
# -----------------------------------------------------------------------------
# 作用：让 docker build context 只包含构建所需的文件
#   - 减小 build context 体积（加速 docker build 第一步的 "sending context"）
#   - 避免缓存失效（node_modules 改了不会让 docker 层缓存失效）
# =============================================================================

# 依赖目录（容器内重装）
node_modules
**/node_modules

# 构建产物（容器内重新构建）
dist
dist-*
frontend/dist
backend/dist
build
*.zip

# 日志
frontend/node_modules
backend/logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# 环境变量（含密钥，绝对不能进镜像）
.env
.env.*
!.env.example

# IDE / 编辑器
.vscode
.idea
*.swp
*.swo
*~

# macOS
.DS_Store

# 测试
coverage
.nyc_output

# Git
.git
.gitignore
.gitattributes

# 文档（运行时不需要）
*.md
!backend/README.md

# Docker 自身文件
Dockerfile
.dockerignore
docker-compose*.yml
